# WEB請求書システム の構築

## システムの目的
- 取引先等に請求関連書類他をWEB公開するシステム
- 請求書関連書類に関する発行、配送、通知、問い合わせの効率化、スピードアップ、郵送の品質向上

## システム利用者
- 管理者：すべての操作が可能。スタッフの管理も含む。
- スタッフ：WEB請求書システムを管理する。取引先からの請求書の問い合わせ対応も行う。スタッフ自身の追加・削除は不可。
- 取引先：WEB請求書システムを利用し、請求書をダウンロード、参照する。
- 請求書発行システム：文書登録APIで、発行した請求書を文書管理データベースへ登録する。API経由のみで画面操作は不要。

## 運用の流れ
### 1. 新規取引先登録
- 取引先：WEB請求書システムのログイン画面から、新規登録をクリックする。
- 取引先：新規取引先登録画面で以下の情報を入力する：
  - 取引先コード（5桁数字、必須、システム内でユニーク）
  - 取引先名（必須）
  - 住所（必須）
  - グループパスワード（PDF暗号化用、必須）※用途説明を画面に表示
  - 担当者情報（最低1名必須、最大3名まで登録可能）
    - 担当者名
    - 担当者メールアドレス（各担当者で異なる必要あり）
    - 担当者パスワード（ログイン用、同じでも可）
- 取引先：登録依頼ボタンをクリックすると、データベースに新規登録依頼が登録される。
  - 取引先コードが既に登録済みの場合はエラーメッセージを表示し、登録を拒否する。
- スタッフ：ダッシュボードに新規登録依頼が表示され、内容を確認する。
  - 承認の場合：登録ボタンをクリックし、以下が自動作成される：
    - フォルダ（請求書キャビネット/取引先コード）
    - グループ（グループID=取引先コード）
    - グループ権限（参照権限）
    - 利用者（担当者1～3名）
    - グループへの利用者登録
    - 承認完了メールを取引先に送信
  - 却下の場合：却下ボタンをクリックし、却下理由を入力。却下理由を含むメールを取引先に送信する。

### 2. 取引先情報変更
- 取引先：ログイン後、登録情報の修正画面にアクセスする。
- 取引先：登録情報の修正画面で以下の情報が表示される：
  - 取引先コード（変更不可、表示のみ）
  - 取引先名（変更可能）
  - 住所（変更可能）
  - グループパスワード（変更可能、即時反映）
  - 担当者情報（最大3名）
    - 既存担当者：チェックボックスで変更対象を選択し、メールアドレス・パスワードを編集可能
    - 追加：空欄の担当者入力欄に新規入力
    - 削除：既存担当者のチェックを入れて削除ボタンをクリック
- 取引先：変更依頼ボタンをクリックすると、データベースに情報変更依頼が登録される。
- スタッフ：ダッシュボードに変更依頼が表示され、内容を確認する。
  - 承認の場合：登録ボタンをクリックし、データベースの情報を更新。承認完了メールを取引先に送信。
  - 却下の場合：却下ボタンをクリックし、却下理由を入力。却下理由を含むメールを取引先に送信する。

### 3. 請求書PDF登録（WEBAPIでの登録）
- 請求書発行システム側：請求書PDFを作成する。
- 請求書発行システム側：WEB請求書システムのWEBAPIで請求書PDF登録を実行する。
  - APIの発行順序：ログインAPI → 登録API → ログアウトAPI
  - 登録APIのパラメータ：
    - 取引先コード（必須、5桁数字）
    - 請求書PDFファイル（必須）
    - PDFファイル名規則：
      - 月次請求書：「月次請求書-請求書番号-請求日.PDF」
      - 随時請求書：「随時請求書-請求書番号-請求日.PDF」
    - 暗号化フラグ（必須、true/false）
      - true：ダウンロード時にグループパスワードでPDFを暗号化
      - false：パスワードなしでダウンロード可能
- WEB請求書システム：請求書PDF登録APIにより以下を実行する：
  - フォルダ自動作成：取引先コード/年度(YYYY)/月(MM)のフォルダが存在しない場合は自動作成
  - 版数管理：同一ファイル名の文書が既に存在する場合は版数をアップ
  - データベースへ登録
  - 取引先へ請求書登録通知メールを送信（ログインURLを含む）

### 4. 請求書PDFダウンロード
- 取引先：請求書PDF登録のメールを受信し、メール本文のURLをクリックしてログイン画面へアクセスする。
- 取引先：ログインID（メールアドレス）、パスワードを入力してログインする。
- 取引先：請求書一覧画面（フォルダツリー表示）で、自分の取引先コード配下の全ての年度・月フォルダと請求書PDFを閲覧できる。
  - 表示形式：年度・月のフォルダツリー構造
  - 権限：参照権限（最新版のみ表示）、ダウンロードのみ可能
- 取引先：該当する請求書PDFをダウンロードし、内容を確認する。
  - 暗号化フラグがtrueの場合：グループパスワードで暗号化されたPDFがダウンロードされる
  - 暗号化フラグがfalseの場合：パスワードなしのPDFがダウンロードされる

### 5. 請求書の内容質問、対応
- 取引先：請求書PDFに確認事項がある場合、電話またはメールでスタッフに問い合わせる。
- スタッフ：取引先からの質問を受付後、WEB請求書システムにログインし、文書検索機能で該当文書を確認する。
  - 検索条件：取引先コード、年度、月
  - 検索結果からPDFのダウンロード・プレビューが可能
  - 管理者権限：全版数の履歴も参照可能
- スタッフ：請求書発行システム側の担当者へも連絡し、原因を確認する。
- スタッフ：必要に応じ、文書管理機能で以下を実行する：
  - 文書登録（API経由または手動）
  - 文書修正（同一ファイル名で再登録すると版数アップ）
  - 文書削除（全版数を一括削除、復元不要）
- 取引先：更新された請求書PDFをダウンロードして確認する。

## 認証IDについて
- 管理者：Django標準の管理者ID（admin）。Django管理画面からスタッフアカウントを管理可能。
- スタッフ：ログインIDはメールアドレス、パスワードでログインする。Django管理画面から管理者が登録。
- 取引先：ログインIDはメールアドレス、パスワードでログインする。取引先コードは5桁数字で、取引先自身が新規登録時に入力する（既存システムとの連携は不要）。
- 請求書発行システム：ログインIDはメールアドレス、パスワードでログインする。1個の共通アカウントをDjango管理画面から管理者が登録。

## パスワードポリシー
- 全てのパスワード（ログイン用、グループパスワード）について：
  - 制限なし（任意の文字列でOK）
  - 有効期限なし
  - パスワードリセット機能なし

##文書管理データベースのイメージ（構造、権限、機能）
- 文書は、論理的なキャビネット、フォルダで管理され、フォルダは、キャビネットに属し、フォルダは、親子関係を持ち、フォルダには、グループのアクセス権限を設定する。ユーザのアクセス権限は、グループに属することで、設定される。
- 取引先の請求書キャビネットの構成イメージは、以下のとおりである。取引先の新規登録で一括して情報を登録する。これにより、文書登録、参照が可能になる。
  フォルダ名は、取引先コード（5桁）とする。アクセス権限を設定するグループIDも、取引先コードとする。そのグループIDに対し、参照権限を付与する。　
  請求書キャビネット--->20000(取引先A)-->20000(グループID-A 取引先Aの参照権限)
                  +-->20001(取引先B)-->20000(グループID-B 取引先Bの参照権限)
  グループID-A:取引先Aの利用者IDを登録
  グループID-B:取引先Bの利用者IDを登録
- 取引先の新規一括登録は、フォルダ、グループ、アクセス権限登録、利用者登録、利用者のグループへの登録を行う。修正は、登録した情報を表示し、変更する。利用者の変更は、変更する利用者をチェックし、チェックした利用者のみ更新する。
- 請求書キャビネットへの文書登録は、文書登録WEBAPIで行う
- 履歴管理：文書は、履歴管理され、同一文書は、版数で管理される。参照権限の場合は、最新版数のみ参照とし、管理者権限は、履歴も参照可能とする
- アクセス権限管理：すべての登録文書は、機密性が高く、権限あるグループに属するユーザのみに公開する
  フォルダに対し、グループのアクセス権限を設定する。アクセス権限は、参照権限、管理者権限とする。
  アクセス権限は、フォルダの親側で設定した権限は、フォルダの子供側に、継承する。両方設定している場合は、権限をマージする。
- 文書暗号化・パスワード設定機能：文書登録時に、暗号化・パスワード設定ありを設定した場合、ダウンロード時に、グループで設定したパスワードがPDF文書に設定される。

## スタッフのダッシュボード
スタッフがログインした際に表示される情報：
- 新規登録依頼一覧（未承認のもの）
  - 表示項目：依頼日時、取引先コード、取引先名、ステータス
  - 操作：承認ボタン、却下ボタン（却下理由入力）
- 変更依頼一覧（未承認のもの）
  - 表示項目：依頼日時、取引先コード、取引先名、変更内容、ステータス
  - 操作：承認ボタン、却下ボタン（却下理由入力）
- 承認済み・却下済みの履歴も表示
- 取引先一覧へのリンク
- 文書管理機能へのリンク
  - 文書検索（取引先コード、年度、月で検索）
  - 文書登録・修正・削除
  - PDFダウンロード・プレビュー

## メール通知
システムから送信するメール通知（パイロット開発のためlocalhost宛に送信）：
1. 請求書PDF登録時 → 取引先へ通知（ログインURL含む）
2. 新規登録承認時 → 取引先へ通知
3. 新規登録却下時 → 取引先へ通知（却下理由含む）
4. 変更依頼承認時 → 取引先へ通知
5. 変更依頼却下時 → 取引先へ通知（却下理由含む）

メール本文は最低限の内容でOK（例：「請求書が登録されました。ログインして確認してください。URL: https://...」）

## 開発言語・DB
- Python、Django、PostgreSQL

## DBアクセス情報
- サーバ名：localhost
- ポート：5432
- DB名：postgres
- ユーザー：postgres
- パスワード：pass
- スキーマ：webseikyu

## 補足事項
- 本システムはパイロット開発である。
- 取引先コードは既存システムで管理されているが、本システムとの連携は不要。
- 請求書発行システムのアカウントは1個の共通アカウント。
